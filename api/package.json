{
  "name": "contest-api",
  "version": "1.0.0",
  "description": "Lambda for contest API",
  "main": "index.js",
  "scripts": {
    "zip": "powershell -NoProfile -Command \"Compress-Archive -Path index.js,node_modules -DestinationPath ..\\contest-api.zip -Force\""
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
